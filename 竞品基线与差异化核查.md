# 竞品基线与差异化核查

> 日期：2026-09-02（初稿）· 2026-09-11（按双主线重构）
> 目的：明确哪些是「轮子」不要再发明，哪些是真正的空位
> 上级：[产品定义与美学纲领.md](产品定义与美学纲领.md) · [选题收敛说明.md](选题收敛说明.md)
> 用途：开题 PPT 第 2、3 页对照案例的出处底稿；论文「相关工作」章节素材

---

## 〇、核查视角的调整（2026-09-11）

初稿的核查视角是「模型适配层在四层里还剩什么空位」，围绕「自适应派」招牌展开。题目删改与双主线确立后，核查视角调整为两条：

1. **交互范式**：各家如何组织「项目 / 会话 / 状态」？（对应 PPT 第 2 页）
2. **Harness 形态**：各家如何组织「上下文 / 能力 / 接入」？（对应 PPT 第 3 页）

原「模型适配层」相关的结论**并入第 2 条**（作为「统一接入」的竞品背景），不再单列。

---

## 一、交互范式核查（PPT 第 2 页底稿）

### 1.1 三家对照

| 产品 | 项目 / 会话组织方式 | 观察到的局限 |
|---|---|---|
| **Codex** | 左侧栏纵向堆叠项目与线程，逐项文字名称列表 | 项目缺少明显的视觉特征，数量一多只能逐项读名称确认（→ 难扫描） |
| **Claude Code Desktop** | 侧边栏列出 Sessions / Messages / Total tokens / Active days；Recents 列表 | 并行 Session 增多后列表很快变长，切换成本随之上升（→ 难扫描 + 占空间） |
| **WorkBuddy** | 左侧栏承载助手 / 专家 / Skill 等多类能力入口 | 多类能力入口并存，侧栏常驻占用横向空间；实际加载策略仍需验证 |

### 1.2 共性结论

- **侧边栏是通用解法，也是共同瓶颈**：项目、线程、会话、能力全部挤在一条纵向列表里，靠文字名称区分。
- **布局固定**：工作区形态不随任务阶段变化，用户无法按阶段或个人习惯调整（→ 自定义程度低）。
- **我们的空位**：把「项目」提升为**可视化的海报卡片**（一眼识别），把「工作区」提升为**可按任务阶段切换的场景**（当前只显示需要的信息）。

---

## 二、Harness 形态核查（PPT 第 3 页底稿）

### 2.1 情境化 Harness 的堆积模式

| 观察到的现象 | 具体表现 | 后果 |
|---|---|---|
| **长提示词反复注入** | 角色设定、场景说明、规则清单随会话反复进入上下文 | 上下文污染——当前任务真正需要的信息占比下降 |
| **能力重复封装** | 助理 / 专家 / Skill 多次封装相近能力 | 能力重复——场景越多，配置与维护越繁琐 |
| **固定流程与细碎规则** | 用流程与规则约束模型行为 | 过度约束——占用模型判断空间，强模型也难灵活调整 |
| **能力与场景强绑定** | 能力围绕具体场景搭建 | 复用困难——换项目或换模型需重新整理整套 Harness |

### 2.2 相关研究与工程进展（可作为论文引用）

| 来源 | 关键结论 | 对本项目的意义 |
|---|---|---|
| LangChain《Tuning Deep Agents to Work Well with Different Models》 | 同一模型在不同 harness 下性能差异显著（tau2-bench 子集 33% → 53%）；纯 harness 层改动可使 Terminal-Bench 2.0 从 52.8% 提升到 66.5%；第一方 harness 不必然最优（Claude Code 配自家 Opus 4.6 排名垫底） | 证明 **Harness 层的投入是可测量的**，为第 3 页「对比实验」方法提供先例 |
| LangChain Deep Agents `harness profiles` | 按模型切换 prompt / 工具命名 / 中间件 / 子代理配置，但**只做配置层**，静态声明、注册时加载 | 说明「减规则、拆模块」这条路尚未被占据 |
| Sonar《The coding personalities of leading LLMs》 | 4,442 个 Java 任务、5 个模型，证实模型存在稳定可测的「编码人格」 | 支撑「不同模型需要不同的 Harness 组织方式」这一命题 |

> ⚠️ 引用前需核对原文与数据口径，避免转述失真。

---

## 三、不要重复发明的基线清单

以下能力是主流产品的标配。**实现即可，不构成创新点**，论文中一句带过或归入「相关工作」。

| # | 基线能力 | 现状 |
|---|---|---|
| 1 | 沙箱与权限分级 | Codex 三档沙箱 × 三种审批策略；Claude 权限模式；Zcode 四档执行模式 |
| 2 | 项目记忆文件 | AGENTS.md / CLAUDE.md / `.trae/rules`——已成事实标准 |
| 3 | 计划模式 | 各家都有（plan mode / `/plan`） |
| 4 | 子代理与并行 | Claude 子代理、Grok 8 并发、Trae SOLO 双智能体 |
| 5 | 扩展机制（MCP / Skills / Hooks / 插件） | 全部具备 |
| 6 | Diff 审阅与变更回滚 | 全部具备 |
| 7 | 会话恢复与分叉 | Claude `/resume` + `/rewind`；DSH resume / fork / replay |
| 8 | Headless / CI / 协议 | Codex `exec`、Claude `-p`、Grok ACP |
| 9 | 上下文压缩 | `/compact` 各家均有 |
| 10 | 成本统计 | `/cost` 各家均有 |
| 11 | 多模型切换 | Trae 会话内切换对比、Zcode 模型选择器 |
| 12 | 基础工具集（文件 / shell / 搜索） | 全部具备 |
| 13 | 浏览器自动化 / 远程控制 | Zcode、Trae 具备 |

### 对本项目的直接影响

| 曾考虑的创意 | 核查结论 |
|---|---|
| **时间轴回放** | ❌ DSH 的 Trajectory view 已实现（resume / fork / replay 共用同一事件流），**不作创新点** |
| **成本仪表 / 路由** | ⚠️ `/cost` 是标配，**降级为标准能力** |
| **变更集 diff 审阅** | ⚠️ diff 审阅是标配，**降级为标准能力** |
| **沙箱边界可视化** | ⚠️ 沙箱是标配，仅保留「越界时提示一次」的克制交互 |
| **意图核对面板** | ✅ **保留**——各家 plan mode 是「方案审批」，无一家做成「理解核对」形态 |
| **克制原则** | ✅ **保留并强化**——七家全部在做加法，无一做减法 |
| **整理操作先预览 + 回滚快照** | ✅ **保留**——文件整理多被当作后台行为，少有「路径级 Diff 预览 + 整体回滚」的前台化设计 |

---

## 四、差异化结论

### 4.1 两条主线的空位

| 主线 | 空位 | 依据 |
|---|---|---|
| **交互系统** | ① 项目入口的**视觉化**（海报卡片替代文字列表）<br>② 工作区按**任务阶段切换场景**（而非固定布局）<br>③ 磁盘整理操作的**前台化预览与回滚** | 第 2 页对照观察 + 第 1 节核查 |
| **Harness 工程** | ① **减规则**而非加规则（轻量上下文）<br>② 能力按**模块**组合而非按场景封装<br>③ 用**量化对比实验**验证组织方式的效果 | 第 3 页四问题 + 第 2.2 节研究先例 |

### 4.2 一句话答辩口径

> 同行普遍在做「加法」——加能力、加规则、加面板；Pioneer 做的是**同一件事的减法**：界面按任务阶段裁剪信息，上下文按任务需要裁剪内容。界面上少显示，模型侧少注入，是同一条克制原则的两面。

---

## 五、待办

1. 补充 Codex / Claude Code Desktop / WorkBuddy 的截图与出处记录（PPT 第 2、3 页对照用）
2. 核对 LangChain 与 Sonar 两份来源的原文数据与口径，写成可引用的「相关工作」小节
3. 复核 DSH Trajectory 的实现方式，确认是否仍有差异化空间
4. 补充 2026 年下半年新出现的同类产品（核查时效性）
