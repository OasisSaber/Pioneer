# Pioneer 环境变量模板 · 复制为 .env（.env 已被 .gitignore 忽略，禁止提交真实值）
#
# Figma REST API（tools/figma-sync.js）
FIGMA_TOKEN=
FIGMA_FILE_KEY=

# Firecrawl（如需抓取脚本；旧 Key 已作废，仅允许使用环境变量注入新 Key）
FIRECRAWL_API_KEY=

# figma-bridge 本地桥接认证（figma-bridge/bridge-server.py；不设置则每次启动生成临时 token）
FIGMA_BRIDGE_TOKEN=
