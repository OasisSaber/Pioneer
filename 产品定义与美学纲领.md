# Pioneer · 产品定义与美学纲领

> 状态：**定稿定位规范稿 v2.0**  
> 定稿日期：2026-09-03（v1.0）· 2026-09-11（题目删改 + 双主线重构）  
> 课题全称：**模块化 AI Agent 交互系统的设计与实现**  
> 系统代号：**Pioneer**（开拓者）  
> 关联：[README.md](README.md) · [选题收敛说明.md](选题收敛说明.md) · [开题PPT逐页文案.md](开题PPT逐页文案.md)

---

## 一、一句话定义与核心哲学

### 1.1 一句话定义

**Pioneer 是一款模块化 AI Agent 交互系统。** 它由两条主线构成——一套可组合的 Agent 交互界面，以及一套轻量、可复用的 Harness 工程——把项目入口、任务执行与 Agent 运行能力组织到同一套桌面工作环境中。

### 1.2 系统哲学三联递进

* **UNIX / Linux** 的理念是 **「一切皆文件」** —— 统一硬件与底层数据的抽象；
* **Harness 框架** 的理念是 **「一切皆插件」** —— 统一模型、工具与运行时的抽象；
* **Pioneer** 的理念是 **「一切皆 UI」(Everything is a UI)** —— 统一 Agent 复杂黑盒与人类直觉认知的桥梁。不把配置、工具、状态和组件藏进代码、配置文件或终端长行中，全部提供可直接看到、直接操作的界面组件。

---

## 二、双主线产品形态

### 2.1 主线一 · 模块化 AI Agent 交互系统

**以本地项目为入口，用项目库和可组合面板组织 Session、输入、状态与输出；布局可随任务和个人习惯调整。**

#### a. 顶层框架：Tabs 多标签体系

* **取代常驻左侧栏**：现有 Agent 在左侧常驻会话历史列表，既占用横向空间，也诱导「单线线性对话」的局限心智。
* **像浏览器一样切页**：媒体库主页、项目工作区、系统设置各为独立标签，多项目多任务并行。

#### b. 工作区管理：媒体库海报墙

* **本地项目元数据读取**：从 Git、README、技术栈信息中读取项目身份，生成海报卡片。
* **海报墙总览**：用户看到的不是路径列表，而是「一眼可辨」的项目卡片——名称、技术栈、活跃状态、Runbook 数集中呈现；点击卡片直接进入对应工作区。
* **整理操作先预览**：重命名、归档等先展示路径 Diff，确认后执行，并保留回滚快照。

#### c. 任务执行：模块化工作区

* **工作区场景切换**：`[输入] [审阅] [输出] [自定]`。其中任务编写与执行前 Intent Review 均属于「输入」；「审阅」专门用于 Agent Files / Diff；「输出」负责 Artifacts；「自定」负责模块化组合。
* **可停靠面板（Tabs / Dockview）**：
  * **意图核对面板**：执行前核对目标与分步方案，**批准执行 / 保持讨论**，批准前不写入任何文件；
  * **执行过程面板**：目标确认、工具调用与结果状态持续可见；
  * **会话分支面板**：可视化呈现会话分叉与回退节点；
  * **输出面板**：产物内联预览、Runbook 提炼与标准文件动作；文件变更 Diff 归入「审阅」场景。
* **常用布局一键切换**：面板自由排列，功能面板按需开启。

### 2.2 主线二 · Harness 工程

**减少情景化提示词和角色封装，把上下文、工具与运行能力拆成可复用模块，为强模型保留更多自主判断空间。**

| 方向 | 说明 |
|---|---|
| **轻量上下文** | 仅提供当前任务所需信息，减少长提示词和重复规则干扰 |
| **能力模块化** | 工具、Skill 与运行能力独立组合，同一能力可跨项目复用 |
| **统一接入** | 通过稳定边界连接模型与 Agent Runtime，降低替换模型和扩展工具的成本 |
| **对比实验** | 在相同模型与任务下，测量不同 Harness 的上下文用量、完成率与耗时 |

**职责边界**：Harness 负责运行底座（复用 Pi Agent Harness 的 `pi-ai` / `agent-core` / `coding-agent`），GUI 负责呈现与操作。

### 2.3 两条主线的关系

不是「前端 + 后端」的简单分层，而是同一产品的两条耦合主线：**界面形态决定 Harness 需要暴露什么，Harness 的边界决定界面能呈现什么。**

---

## 三、核心美学：克制原则 (Restraint Principle)

> **「一切皆 UI」不等于「UI 元素的粗暴堆砌」**。Pioneer 严格践行克制原则：**功能默认沉默，只在必要时浮现；证明靠对比而非堆砌；不做无意义的信息陈列。**

### 三条细则

1. **沉默优先** —— 默认不打扰。任何 UI 元素浮现前，必须回答：**「它凭什么现在出现？」** 答不出就不出现。
2. **渐进披露** —— 默认单行叙事短句与轻量胶囊，深度信息通过二级展开呈现。
3. **证明靠对比** —— 不靠动画或信息密度证明能力，靠执行前后状态 Diff、方案核对与**量化实验数据**体现价值。

### 在双主线中的落点

* **交互系统**：PPT 第 6 页右侧那句「每个阶段只显示当前需要的信息」，就是克制原则的工程化表述——工作区场景切换本质上是**按阶段裁剪信息**。
* **Harness 工程**：「轻量上下文」是克制原则在模型侧的同一件事——**按任务裁剪上下文**。界面裁剪给人看，上下文裁剪给模型看，是同一条原则的两面。

> 这条贯通是答辩最值得展开的一节：**设计方法论与工程方法论的统一**。

---

## 四、核心模块在「一切皆 UI」下的形态

| 模块 | 传统 Agent 现状 | Pioneer 实践 |
|---|---|---|
| **工作区 / 项目** | 文本路径或侧边栏列表 | **媒体库海报墙**：卡片集中呈现名称、技术栈与活跃状态 |
| **工作区布局** | 固定的三栏对话窗口 | **模块化工作区**：Tabs + 可停靠面板，按任务阶段切换场景 |
| **能力 / 工具** | 藏在 JSON / 代码 / 提示词里 | **可见的能力入口与模块化组合** |
| **意图与计划** | 直接执行，缺少核对 | **意图核对面板**：目标与分步方案可见，批准前不写入 |
| **执行过程** | 滚不到底的终端输出 | **执行过程面板**：目标、工具调用与结果状态持续可见 |
| **任务成果** | 纯文字「已修改文件」 | **输出**：Artifacts 内联与文件动作；Diff 独立归入「审阅」 |

---

## 五、开题题目与答辩支撑

* **定稿题目**：**《模块化 AI Agent 交互系统的设计与实现》**
* **数媒四合一对应**：
  * **设计**：媒体库海报墙 + 模块化工作区场景 + 克制原则（可论证的信息设计方法论）；
  * **技术**：Tabs / Dockview 面板系统、显式状态层与事件流、Harness 上下文组装与统一接入；
  * **内容**：项目元数据读取规范、Runbook 模板、场景内容包、交互短句库；
  * **智能**：意图核对机制、轻量上下文策略、多模型接入、能力模块化组合。

---

## 六、历史探索记录（未纳入本次开题范围）

> 以下为 2026-09-02 前后的探索结论。**保留记录、不做删除**，但**本次开题 PPT 未展开、不作为交付承诺**。若后续有余力可重新评估。

### 已并入双主线

| 探索项 | 现归属 |
|---|---|
| **整理闸门**（识别 → Diff 提案 → 逐条批准 → 变更集回滚） | → 交互系统 A2「整理操作先预览」 |
| **意图卡**（各家 plan mode 是「方案审批」，无一家做成「理解核对」） | → 交互系统 A3「意图核对面板」 |
| **克制原则**（七家全部做加法，无一做减法） | → 本文件第三节，已升格为纲领 |
| **多模型接入** | → Harness 工程「统一接入」 |

### 暂不纳入

| 探索项 | 状态 | 备注 |
|---|---|---|
| **TUI 双端** | 暂不纳入 | 开题 PPT 只呈现 GUI；TUI 保留在技术可能性中 |
| **手机远程控制**（PWA 先行、零账号自组网） | 暂不纳入 | 成本与风险已记录，未进入本次范围 |
| **多端时间观**（不同屏幕给人不同的时间权限） | 暂不纳入 | 作为架构论点保留，可留待论文讨论 |
| **语音汇报人 / 本地 TTS** | 暂不纳入 | 数字人方向谨慎，避免 AI 味 |
| **模型适配层 / 行为策略库** | 降级 | 并入 Harness「统一接入」，不再作为招牌 |

---

## 七、待办（不排序）

1. 补高保真交互原型：海报墙、模块化工作区、意图核对三张关键屏
2. 定义对比实验的最小可执行方案（任务集、模型、测量口径）
3. 定「整理 Diff 预览 + 回滚快照」的数据结构与交互细节
4. 确认 Pi Agent Harness 的接入边界与可复用能力清单
5. 补「用户故事库」（需求分析 / 用户研究环节最缺）
