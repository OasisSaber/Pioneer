# 开题 PPT 逐页文案（Pioneer · 10 页 · 对齐 Figma 定稿）

> 用途：10 页开题汇报的**内容层**（页面大字 + 逐页讲稿 + 视觉映射）  
> 权威源：Figma `Pioneer-PPT`（导出版 `开题汇报(4).pptx`，2026-09-11）  
> 时长：3–5 分钟（总讲稿约 4 分 20 秒）；每页已标注用时  
> 视觉风格：深色封面 / 浅色内页 · 大字号标题 · 白卡 + 浅灰底 · 蓝色唯一点缀 · 去仪表盘 AI 味  
> 题目口径：**《模块化 AI Agent 交互系统的设计与实现》**（封面与英文副标待回 Figma 修正，见第 1 页备注）

---

## 第 1 页 · 课题信息（约 20 秒）

**页面大字**
- 主标题：**模块化 AI Agent 交互系统 / 的设计与实现**
- 英文副标：`DESIGN AND IMPLEMENTATION OF A MODULAR AI AGENT INTERACTION SYSTEM`
- 眉标：`PIONEER · DESKTOP AGENT` / `2027 届数字媒体技术专业毕业设计`
- 三联宣言：`Linux: 一切皆文件 · DeepseekHarness: 一切皆插件 · Pioneer: 一切皆 UI`
- 信息栏：演示用户 · 示例班级 / 学号 2026000000 / 指导教师 / 方向五 · 人工智能与数字人 / Agent 交互系统
- 核心形态：Jellyfin 式项目海报墙 / Premiere Pro 式模块化工作区 / 模块化 Harness 工程

> 备注：Figma 现版封面为「模块化 AI Agent 交互系统**及其 Harness 工程**的设计与实现」。按 2026-09-11 定稿题目，需删去「及其 Harness 工程」，英文副标同步删去 `AND ITS HARNESS ENGINEERING`。Harness 保留在第 4 页起的正文中。

**讲稿（约 20 秒）**
> 各位老师好，我的毕业设计课题是「模块化 AI Agent 交互系统的设计与实现」，项目代号 Pioneer。它由两条主线构成：一套可组合的 Agent 交互界面，以及一套轻量、可复用的 Harness 工程，两者共同服务本地开发任务。核心理念是「一切皆 UI」——让项目入口、Agent 状态和扩展能力都能在界面中直接看到、直接操作。

---

## 第 2 页 · 选题背景（一）· 交互层（约 30 秒）

**页面大字**
- 主标题：**主流 Agent 应用仍依赖侧边栏管理项目与对话，内容一多就难找、难读、难切换。**
- 副标：同时维护多个本地项目时，项目、线程和任务会持续累积；侧边栏越拉越长，对话、工具与状态也被固定在同一种布局里。
- 对照案例：
  - **Codex**：项目与线程纵向堆叠，主要靠文字名称辨认
  - **Claude Code Desktop**：并行 Session 增多后，列表很快变长，切换成本随之上升
- 三个痛点：
  - `01 难扫描` —— 项目缺少明显的视觉特征，数量一多，只能逐项读名称确认
  - `02 占空间` —— 侧栏常驻会持续占用横向空间，真正用于任务的区域随之变窄
  - `03 自定义程度低` —— 固定的对话窗口和面板位置难以按个人习惯调整，也难随任务阶段改变布局

**讲稿**
> 当前 Agent 的能力在快速提升，但交互形态几乎没有变。主流产品仍然把项目、线程和会话塞进一条纵向侧边栏里——项目一多，就只能靠读文字名称去辨认；侧边栏常驻又持续挤压真正用于任务的横向空间。更关键的是，界面布局是固定的，很难按任务阶段或个人习惯调整。当同时维护多个本地项目时，这三件事会叠加放大。

---

## 第 3 页 · 选题背景（二）· Harness 层（约 30 秒）

**页面大字**
- 主标题：**情景特化 Harness 持续堆积，开始限制高能力模型发挥。**
- 副标：本项目关注冗长规则、重复封装与任务无关信息的潜在成本；拟在相同模型与任务下，对比不同 Harness 的上下文用量、完成率与耗时。
- 对照案例：**WorkBuddy** —— 界面观察：多类能力入口并存，实际加载策略仍需验证
- 四个问题：
  - `01 上下文污染` —— 长提示词和角色说明反复进入上下文，当前任务真正需要的信息占比越来越低
  - `02 能力重复` —— 助理、专家和 Skill 多次封装相近能力，场景越多，配置和维护越繁琐
  - `03 过度约束` —— 固定流程和细碎规则占用模型判断空间，强模型也更难根据任务灵活调整策略
  - `04 复用困难` —— 能力与场景绑得过紧，换项目或换模型时，经常需要重新整理整套 Harness

**讲稿**
> 第二个问题在工程侧。为了适配不同场景，Harness 会被不断加东西：提示词越来越长、角色和 Skill 反复封装、固定流程越铺越多。结果是上下文被大量无关信息占据，模型的判断空间被规则挤掉，而这些能力又和场景绑得太死，换个项目就得重新整理一遍。所以我把 Harness 工程也作为一条主线：不是继续加，而是把上下文、工具和运行能力拆成可复用模块，并计划用相同模型、相同任务去对比不同 Harness 的上下文用量、完成率和耗时。

---

## 第 4 页 · 设计目标与主要内容（核心页，约 35 秒）

**页面大字**
- 主标题：**PIONEER：模块化 AI Agent 交互系统与 Harness 工程**
- 副标：Pioneer Agent 同时推进两部分：一套可组合的 Agent 交互界面，以及一套轻量、可复用的 Harness 工程。两者共同服务本地开发任务。
- 左卡 `INTERACTION SYSTEM` · **模块化 AI Agent 交互系统**
  - 以本地项目为入口，用项目库和可组合面板组织 Session、输入、状态与输出；布局可随任务和个人习惯调整。
  - `01 项目入口` —— 海报卡片集中显示项目信息，跨工程定位和进入更直接
  - `02 模块化工作区` —— Tabs 与 Dockview 组合面板，任务变化时可快速重排工作区
  - `03 执行过程` —— 目标确认、工具调用与结果状态持续可见，方便随时核对当前进度
- 右卡 `HARNESS ENGINEERING` · **Harness 工程的设计与实现**
  - 减少情景化提示词和角色封装，把上下文、工具与运行能力拆成可复用模块，为强模型保留更多自主判断空间。
  - `01 轻量上下文` —— 仅提供当前任务所需信息，减少长提示词和重复规则干扰
  - `02 能力模块化` —— 工具、Skill 与运行能力独立组合，同一能力可跨项目复用
  - `03 统一接入` —— 通过稳定边界连接模型与 Agent Runtime，降低替换模型和扩展工具的成本

**讲稿**
> 基于这两个问题，Pioneer 的设计目标分成两条主线。左边是交互系统：用海报卡片做项目入口，让人一眼看清是什么项目、有多活跃；进入项目后用 Tabs 和可停靠面板组成工作区，任务变了就重排布局；执行过程中，目标、工具调用和结果状态始终可见，可以随时核对。右边是 Harness 工程：上下文只给当前任务需要的部分；工具、Skill 和运行能力独立组合、跨项目复用；并通过稳定边界连接模型与运行时，降低换模型和加工具的成本。两条线不是简单的前端加后端，界面决定 Harness 要暴露什么，Harness 的边界决定界面能呈现什么。

---

## 第 5 页 · 设计方案 · 海报墙（约 30 秒）

**页面大字**
- 主标题：**工作区管理：把本地项目变成「媒体库」**
- 副标：自动读取 Git、README 和技术栈信息，用海报卡片组织本地工程；涉及整理和改名时，先预览变更，再确认执行。
- 界面示意：**本地工作区海报墙**（`D:\Project` · 6 个活跃工程）
  - 卡片要素：项目海报封面 · 技术栈胶囊（`React·TS` / `Unity·HMI` / `Python·FFmpeg`）· 活跃时间戳 · `GIT: clean` · `N Runbooks`
  - 顶部 Tabs：`[ 工作区 ] [ Pioneer ] …` ／ 右上角标：本地缓存
  - 底部注：界面设计示意 · 非实时运行截图
- 右侧 `DESIGN VALUE`：
  - **让项目一眼可辨，点击即达** —— 名称、技术栈、最近活跃直接出现在卡片上，减少在目录与路径之间来回寻找
  - `01 一眼识别` —— 名称、技术栈和活跃状态集中展示
  - `02 直接进入` —— 从项目卡片进入对应工作区
  - `03 历史可追溯` —— Runbook 与最近执行记录持续沉淀
  - **整理操作先预览** —— 重命名、归档等操作先展示路径 Diff，确认后执行，并保留回滚快照

**讲稿**
> 这是工作区管理的第一条设计。我们不把本地项目做成路径列表，而是自动读取 Git、README 和技术栈信息，渲染成海报卡片——项目是什么、用什么技术、最近有没有动，一眼就能看到，点击直接进入对应工作区。涉及重命名、归档这类会改动磁盘的操作，系统不会直接执行，而是先给出路径级的 Diff 预览，确认之后才写入，并且保留回滚快照。

---

## 第 6 页 · 设计方案 · 模块化工作区（核心演示页，约 30 秒）

**页面大字**
- 主标题：**任务执行：从「聊天框」转向模块化工作区**
- 副标：任务阶段变化时，工作区随之切换：先核对目标，再观察执行过程，最后检查输出结果。
- 界面示意：Pioneer 工作区
  - 顶层 Tabs：`[ 工作区 ] [ Pioneer ] [ Supersonic ] [ … ] [ + ]`
  - 工作区场景栏：`[ 意图审阅 ] [ 专注执行 ] [ 状态全景 ] [ 成果画廊 ]`
  - 面板组合：项目资源 / 会话分支 ｜ Agent 对话 · 目标与步骤 ｜ 任务模型 / 执行记录
  - 关键交互：**批准执行 / 保持讨论**（「批准后不会写入任何文件」）
  - 底部注：界面设计示意 · 非实时运行截图
- 右侧 `WORKFLOW SHIFT`：
  - **工作区随任务切换** —— 工作区面板自由排列 · 功能面板按需开启 · 常用布局一键切换
  - 目标很简单：**每个阶段只显示当前需要的信息。**

**讲稿**
> 第二条设计是任务执行。我们不做单一的聊天窗口，而是把工作区按任务阶段组织成可切换的场景：在「意图审阅」里，Agent 先说明它读到了什么、打算改哪几个文件，由用户批准执行或者继续讨论——批准之前不会写入任何文件；切到「专注执行」，界面只保留过程与状态；任务收尾再切到成果画廊检查结果。工作区的面板可以自由排列，也可以一键切换常用布局。原则很简单：每个阶段只显示当前需要的信息。

---

## 第 7 页 · 技术与工具（约 25 秒）

**页面大字**
- 主标题：**技术栈：先验证交互，再工程化接入 Agent**
- 副标：当前原型用 Figma 和原生 Web 技术验证交互；正式工程计划以 React/TypeScript 实现界面，并复用 Pi 的 Agent 运行能力。
- `01 · 交互原型 / PROTOTYPE`
  - **Figma + HTML / CSS / JS** —— 用 1920×1080 单文件 Demo 验证核心界面和任务闭环，先把交互跑通，再进入正式工程。
  - 标签：`Tabs` `海报墙` `场景切换` `意图核对` `事件流` `成果审阅`
  - 特点：零 npm / build / CDN，`file://` 可直接运行
- `02 · 界面工程 / BUILD`
  - **React + TypeScript** —— 把已经验证的信息架构、组件和状态模型迁入正式工程，保留可组合工作区与场景切换。
  - `组件化界面` 沿用已验证的 IA 与关键交互 ／ `模块化工作区` 面板按任务组合与切换 ／ `显式状态层` UI 状态与模型上下文分离
- `03 · AGENT HARNESS / REUSE`
  - **Pi Agent Harness** —— 计划复用模型适配、Agent Loop、工具执行和会话能力；Pioneer 负责 GUI 与人机协同。
  - `pi-ai` 多 Provider 模型适配 ／ `agent-core` 状态、工具执行、事件流 ／ `coding-agent` Session、Compaction、SDK / RPC
  - 职责边界：**Harness 负责运行底座，GUI 负责呈现与操作**
- 底部条：工程与验证 `jj + Git/GitHub` · `Node.js E2E`；原则：底座保持通用，场景能力按需组合，减少特化规则对强模型判断的干扰。

**讲稿**
> 技术选型分三层，节奏是「先验证交互，再工程化接入」。第一层用 Figma 加原生 HTML/CSS/JS 做单文件原型，零构建、可直接双击打开，用来快速验证界面结构和任务闭环；第二层把验证过的信息架构和组件迁入 React 加 TypeScript 的正式工程；第三层接入 Pi Agent Harness，复用它的模型适配、Agent Loop、工具执行和会话能力。职责边界很清楚：Harness 负责运行底座，GUI 负责呈现与操作。

---

## 第 8 页 · 预期成果（约 20 秒）

**页面大字**
- 主标题：**预期成果：一个可运行系统 + 一套完整设计资产**
- 副标：最终交付以可演示的核心任务闭环为主，同时整理设计规范、源代码、演示视频和毕业设计材料。
- 交付目录：
  - `demo/` `A01 · PRIMARY` **桌面交互系统原型** —— 海报墙 + 模块化工作区完整闭环
  - `spec/` `A02 · SPEC` **设计系统规范** —— Design Tokens · 组件 · 交互规则
  - `plugin/` `A03 · TOOLS` **核心插件库** —— 本地文件 / 工程管理基础工具
  - `video/` `A04 · VIDEO` **演示视频** —— 高保真操作录屏与答辩演示
  - `paper/` `A05 · PAPER` **毕业设计材料** —— 开题 · 中期 · 论文 · 答辩
  - `src/` `A06 · SOURCE` **完整源代码** —— 前端界面 + 后端运行时
- 验收目标：
  - **可定制** —— GUI 与 Harness 均可模块化组合，支持按任务和习惯调整
  - **完整性** —— 项目入口、任务执行与结果检查形成完整闭环
  - **一致性** —— 界面语言、组件规范和交互规则保持统一

**讲稿**
> 最终交付六项：可运行的桌面交互系统原型、设计系统规范、核心插件库、演示视频、毕业设计材料和完整源代码。验收按三条标准：可定制——界面和 Harness 都能模块化组合；完整性——从项目入口到任务执行再到结果检查形成闭环；一致性——界面语言、组件规范和交互规则保持统一。

---

## 第 9 页 · 当前进展（约 20 秒）

**页面大字**
- 主标题：**当前进展：GitHub、Figma 与 Harness 方案同步推进**
- 副标：GitHub 已建立工程与文档基线，Pioneer UI 持续在 Figma 中迭代；Harness 方案同步梳理运行边界和接入方式。
- 左卡 `REPOSITORY / MAIN` · **GitHub · 工程与文档进入版本管理**
- 右卡 `DESIGN / IN PROGRESS` · **Figma · Pioneer UI 持续迭代**
- 时间轴（计划安排 · 2026.09 — 2027.05）：
  - `2026-09-10` **题目 / 架构校准** ← 当前
  - `2026.10-12` Pioneer UI 原型
  - `2026.12-2027.02` Harness / 插件
  - `2027.02-04` 事件流联调 / 测试
  - `2027.04-05` 论文 / 答辩 / 归档
- 底部注：优先完成项目入口 → Agent 对话 → 批准 → 结果检查的闭环，插件扩展和高级自动化作为后续增强。

**讲稿**
> 目前三条线同步推进。GitHub 上已经建立了工程与文档基线，版本管理用 jj 加 Git；Pioneer 的界面在 Figma 中持续迭代；Harness 这边在梳理运行边界和接入方式。节奏上，今年内完成 UI 原型，明年初接入 Harness 和基础插件，之后做事件流联调与测试。优先级很明确：先把项目入口、对话、批准、结果检查这条闭环跑通，插件扩展和高级自动化放到后面。

---

## 第 10 页 · 总结（约 20 秒）

**页面大字**
- 眉标：`PIONEER · EVERYTHING IS A UI`
- 主标题：**模块化 AI Agent GUI / 面向高能力模型的轻量 Harness**
- 副标：Pioneer 面向多项目开发场景，把项目入口、任务执行和 Agent 运行能力组织到同一套桌面工作环境中。
- 三行收束：
  - **场景** —— 海报墙负责项目入口，模块化工作区承载任务过程；轻量 Harness 适配高能力模型
  - **方法** —— 前端以 Figma 完成设计验证，正式工程采用 React / TypeScript，并接入 Pi Agent Harness
  - **交付** —— 交付可运行原型、设计稿、源代码、演示视频和毕业设计论文
- 结语：感谢各位老师的指导与聆听 · `THANK YOU · QUESTIONS & FEEDBACK`

**讲稿**
> 总结一下：Pioneer 做两件事——用海报墙和模块化工作区把 Agent 的交互做清楚，用轻量的 Harness 把上下文和能力组织做干净，让强模型有更多发挥空间。交付一份可运行原型、一套设计资产和完整论文。感谢各位老师，请批评指正。

---

## 附录 A · 答辩 7 问速答清单

| 教师提问方向 | 核心回答要点 |
|---|---|
| **1. 为什么选这个题？** | 现有 Agent 界面高度同质化：项目与对话挤在一条侧边栏里，难扫描、占空间、布局固定；同时 Harness 不断堆砌情景化规则，反过来限制强模型发挥。 |
| **2. 系统主要做什么？** | 两条主线：交互系统负责项目入口（海报墙）、任务执行（模块化工作区）与状态呈现；Harness 工程负责上下文、工具与运行能力的模块化组织。 |
| **3. 最核心的创新与自主工作量？** | 模块化交互界面（Tabs + Dockview 组合面板与场景切换）＋ 执行前意图核对机制 ＋ Harness 的轻量上下文与能力模块化设计 ＋ 可测量的对比实验。 |
| **4. 用什么技术实现？** | 原型：Figma + HTML/CSS/JS（零构建）；工程：React + TypeScript；底座：接入 Pi Agent Harness（`pi-ai` / `agent-core` / `coding-agent`）。 |
| **5. 和市面工具有何特色/差异？** | 不做单一聊天框，界面按任务阶段切换；项目以海报卡片组织而非路径列表；整理等磁盘操作先预览 Diff 再执行；Harness 以「减规则、留判断空间」为方向而非继续堆砌。 |
| **6. 最终验收交付什么？** | 可运行桌面交互系统原型 + 设计系统规范 + 核心插件库 + 演示视频 + 完整源代码 + 毕业论文。 |
| **7. 如果时间不够怎么删减？** | P0 保底：项目入口（海报墙）+ 模块化工作区场景切换 + 意图核对与结果检查闭环。插件库按需渐进扩展；高级自动化与自定义布局保存可后置。 |

## 附录 B · 与旧版的差异留痕（2026-09-11）

| 项 | 旧版（8–10 页） | 现版（10 页） |
|---|---|---|
| 题目 | 基于模块化架构的桌面任务助理 Agent 交互系统设计与实现 | 模块化 AI Agent 交互系统的设计与实现 |
| 主线 | 「一切皆 UI」单主线 | 交互系统 ＋ Harness 工程双主线 |
| 选题背景 | 1 页泛化「三大交互困境」 | 2 页：交互层（Codex / Claude Code Desktop 对照）＋ Harness 层（WorkBuddy 对照） |
| 佐证方式 | 「90%+ 桌面 Agent」等无出处数字 | 真实产品界面实拍对照 |
| 品牌表述 | Jellyfin 影视库 / Premiere Pro 模块化工作区 | 媒体库 / Tabs + Dockview（品牌名淡出） |
| 技术栈 | 单页混合罗列 | 三层：原型 → 工程 → Harness（复用） |
| 成果 | 6 项清单 | 6 个交付目录 + 3 条验收目标 |
| 当前进展 | 无 | 新增第 9 页（GitHub / Figma / Harness 三线 + 时间轴） |
| 页数 | 8–10 页 | 10 页 |
